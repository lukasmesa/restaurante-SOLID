<% include partials/header.ejs %>
<div class="container">
    <% if (message) { %>
        <p class="text-center text-danger"><%= message %></p>
    <% } %>

    <% if (categoria) { %>
        <form class="add-categoria-form" action="" method="post">
            <div class="form-row">
                <div class="form-group col-md-4">
                    <label for="codigo">c&oacute;digo</label>
                    <input type="number" class="form-control" name="codigo" id="codigo" value="<%= categoria.codigo %>" required>
                </div>
                <div class="form-group col-md-4">
                    <label for="nombre">Nombre</label>
                    <input type="text" class="form-control" name="nombre" id="nombre" value="<%= categoria.nombre %>" required>
                </div>
            </div>
            <button type="submit" class="btn btn-success float-right">Actualizar categoría</button>
        </form>
    <% } else { %>
        <p class="text-center">No se encontraron categorias. Vaya <a href="/add" >aqu&iacute;</a> para agregar categorias.</p>
    <% } %>
</div>
</div>
</body>
</html>